{"build": {"builder": "NIXPACKS", "buildCommand": "uv sync --frozen --no-dev"}, "deploy": {"startCommand": "uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT}", "healthcheckPath": "/health", "healthcheckTimeout": 300}}
