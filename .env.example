# ================================================================
# Hana Voice SaaS - Environment Configuration
# ================================================================
# Copy this file to .env.local and fill in your actual values

# ================================================================
# SUPABASE CONFIGURATION (Required)
# ================================================================
# Get these from your Supabase project dashboard -> Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ================================================================
# AUTHENTICATION CONFIGURATION (Required)
# ================================================================
# JWT secret for signing tokens (generate a secure random string)
JWT_SECRET_KEY=your_secure_jwt_secret_key_here

# ================================================================
# ADMIN ACCESS (Optional)
# ================================================================
# Admin credentials for direct API access during development
ADMIN_USERNAME=hana_admin
ADMIN_PASSWORD=secure_admin_password_here

# ================================================================
# PYTHON VOICE SERVICE CONFIGURATION (Required)
# ================================================================
# Python voice service authentication and settings
VOICE_SERVICE_SECRET=your_voice_service_secret_key

# Voice service URL (for health checks and API calls)
# Development: http://localhost:8000
# Production: Use Railway reference variables (see railway.toml)
VOICE_SERVICE_URL=http://localhost:8000

# Voice processing limits
MAX_CONCURRENT_SESSIONS=5
RATE_LIMIT_PER_MINUTE=30
LOG_LEVEL=INFO

# ================================================================
# TELEPHONY INTEGRATION (Optional - for Maqsam)
# ================================================================
# Maqsam telephony provider API key (needed for live calling)
TELEPHONY_TOKEN=pre-shared-token-from-maqsam

# ================================================================
# REMOVED VARIABLES (No longer used)
# ================================================================
# These variables were removed because the current implementation uses:
# - Vosk Arabic STT (instead of OpenAI)
# - Coqui XTTS Arabic TTS (instead of OpenAI)
# - Maqsam telephony (instead of FreePBX)

# REMOVED: OPENAI_API_KEY - Not needed (using local Arabic models)
# REMOVED: FREEPBX_HOST - Not used (switched to Maqsam)
# REMOVED: FREEPBX_USERNAME - Not used (switched to Maqsam)
# REMOVED: FREEPBX_PASSWORD - Not used (switched to Maqsam)
# REMOVED: VOICE_SERVICE_TOKEN - JWT not required for internal service auth
# REMOVED: NEXTAUTH_URL - Not referenced in code
# REMOVED: NODE_ENV - Automatically handled by Next.js
# REMOVED: NEXT_PUBLIC_API_URL - Not referenced in code

# ================================================================
# IMPORTANT NOTES:
# ================================================================
#
# 1. NEVER commit actual values to version control
# 2. Use different values for development/staging/production
# 3. Keep API keys and secrets secure
# 4. Use environment-specific Supabase projects
# 5. Rotate JWT secrets regularly
# 6. Use strong, unique passwords
#
# ================================================================
# SETUP INSTRUCTIONS:
# ================================================================
#
# 1. Copy this file to .env.local
# 2. Fill in required values (marked with *)
# 3. For production, deploy via render.yaml
# 4. Use secure random strings for secrets
# 5. Test all values before deployment
# ================================================================
