# =================================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# This file contains only essential variables for production deployment
# =================================================================================

# =================================================================================
# FRONTEND (Next.js) - Required for build and runtime
# =================================================================================

# Supabase Configuration (REQUIRED - Replace with your actual values)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Voice Service Communication (REQUIRED - Replace with actual values)
VOICE_SERVICE_URL=http://localhost:8000
VOICE_SERVICE_SECRET=your_voice_service_secret_key

# Authentication (REQUIRED - Generate secure values)
JWT_SECRET_KEY=your_secure_jwt_secret_key_here

# Admin Access (OPTIONAL - For demo/testing only)
ADMIN_USERNAME=hana_admin
ADMIN_PASSWORD=secure_admin_password_here

# =================================================================================
# BACKEND (Python Voice Service) - Required for voice processing
# =================================================================================

# Voice Service Configuration (REQUIRED)
NEXTJS_API_URL=http://localhost:3000
TELEPHONY_TOKEN=your_maqsam_token_here
LOG_LEVEL=INFO

# =================================================================================
# DEPLOYMENT NOTES
# =================================================================================
#
# FRONTEND VARIABLES (set in Railway Next.js service):
# - NEXT_PUBLIC_SUPABASE_URL        → Your Supabase project URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY   → Your Supabase anon key
# - VOICE_SERVICE_URL               → http://${{hana-voice-service.RAILWAY_PRIVATE_DOMAIN}}:${{hana-voice-service.PORT}}
# - VOICE_SERVICE_SECRET            → Same value as backend
# - JWT_SECRET_KEY                  → Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - ADMIN_USERNAME / ADMIN_PASSWORD → For demo access only
#
# BACKEND VARIABLES (set in Railway Python service):
# - VOICE_SERVICE_SECRET            → Same value as frontend (shared secret)
# - NEXTJS_API_URL                  → http://${{hana-voice-saas.RAILWAY_PRIVATE_DOMAIN}}
# - TELEPHONY_TOKEN                 → Your Maqsam token (if using telephony)
# - LOG_LEVEL                       → INFO/DEBUG/WARNING
#
# =================================================================================
# DEPRECATED VARIABLES (DO NOT USE - Listed here for reference only)
# =================================================================================
# OPENAI_API_KEY      → ❌ Replaced by local Arabic models (Vosk + Coqui XTTS)
# FREEPBX_HOST        → ❌ Replaced by Maqsam telephony
# FREEPBX_USERNAME    → ❌ Replaced by Maqsam telephony
# FREEPBX_PASSWORD    → ❌ Replaced by Maqsam telephony
# VOICE_SERVICE_TOKEN → ❌ Not used (authentication via VOICE_SERVICE_SECRET)
# NEXT_PUBLIC_API_URL → ❌ Not referenced in current code
# TELEPHONY_SERVICE_URL → ❌ Not needed with Maqsam
# =================================================================================
