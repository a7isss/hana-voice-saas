# =======================================================================
# RAILWAY DEPLOYMENT - ENVIRONMENT VARIABLES
# Copy this file and fill in the values for your Railway deployment
# =======================================================================

# =======================================================================
# DATABASE (Supabase PostgreSQL)
# =======================================================================
# Get these from your Supabase project settings
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Direct database connection (for migrations)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres

# =======================================================================
# NEXT.JS APPLICATION
# =======================================================================
NODE_ENV=production

# Application URLs (Railway will provide these)
NEXT_PUBLIC_API_URL=https://hana-voice-saas.up.railway.app
VOICE_SERVICE_URL=https://hana-voice-service.up.railway.app

# JWT Authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET_KEY=generate_a_secure_random_string_here

# Admin Credentials
ADMIN_USERNAME=hana_admin_2024
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ADMIN_PASSWORD=generate_a_secure_password_here

# =======================================================================
# VOICE SERVICE (Python FastAPI)
# =======================================================================
# Shared secret between Next.js and Python service
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VOICE_SERVICE_SECRET=hana_voice_shared_secret_2024_secure_key

# Voice service authentication token
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VOICE_SERVICE_TOKEN=generate_a_secure_token_here

# Voice model configuration
VOSK_MODEL_PATH=/data/models/vosk-model-ar-0.22-linto-1.1.0
TTS_MODEL_PATH=/data/models/tts

# Service configuration
LOG_LEVEL=INFO
MAX_CONCURRENT_SESSIONS=10
RATE_LIMIT_PER_MINUTE=60

# =======================================================================
# TELEPHONY (Maqsam Integration)
# =======================================================================
# Get these from your Maqsam account
MAQSAM_API_URL=https://api.maqsam.com
MAQSAM_API_TOKEN=your_maqsam_token_here
MAQSAM_PHONE_NUMBER=+966XXXXXXXXX

# =======================================================================
# OPTIONAL: MONITORING & ANALYTICS
# =======================================================================
# Sentry for error tracking (optional)
# SENTRY_DSN=your_sentry_dsn_here

# Google Analytics (optional)
# NEXT_PUBLIC_GA_ID=your_ga_id_here

# =======================================================================
# DEPLOYMENT NOTES
# =======================================================================
# 1. Never commit this file with real values to git
# 2. Set these variables in Railway dashboard under each service
# 3. Use Railway's built-in secrets management
# 4. Rotate secrets regularly for security
# 5. Use different values for staging and production
# =======================================================================

# =======================================================================
# HOW TO GENERATE SECURE SECRETS
# =======================================================================
# Run these commands to generate secure random strings:
#
# For JWT_SECRET_KEY:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# For ADMIN_PASSWORD:
#   node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
#
# For VOICE_SERVICE_SECRET:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# For VOICE_SERVICE_TOKEN:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# =======================================================================
